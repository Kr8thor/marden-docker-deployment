{
  "version": 2,
  "routes": [
    { "src": "/api/(.*)", "dest": "https://marden-backend.vercel.app/api/$1" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "build": {
    "env": {
      "VITE_API_URL": "https://marden-backend.vercel.app/api"
    }
  }
}
