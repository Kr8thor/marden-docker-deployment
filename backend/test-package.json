{
  "name": "test-backend",
  "version": "1.0.0",
  "description": "Simple test backend for deployment",
  "main": "test-app.js",
  "scripts": {
    "start": "node test-app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
